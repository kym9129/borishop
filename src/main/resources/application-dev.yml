spring:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop # SessionFactory 실행 시 Drop, Create, Alter 실행. 종료 시 Drop
    properties:
      hibernate:
        format_sql: true
        show_sql: true